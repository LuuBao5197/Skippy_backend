class EmployeeDto {
  constructor(name, email, username, phoneNumber, ) {
    this.name = name; 
    this.email = email; 
    this.phoneNumber = phoneNumber; 
    this.username = username;
    // this.cin = cin; // SO CCCD
    // this.avatarUrl = avatarUrl;
  }
}